name: export

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  export:
    name: export
    runs-on: ubuntu-latest
    container:
      image: barichello/godot-ci:4.6.1
    steps:
      - uses: actions/checkout@v4
      - name: Install export templates
        run: |
          mkdir -p ~/.local/share/godot/export_templates
          mv /root/.local/share/godot/export_templates/* ~/.local/share/godot/export_templates/ 2>/dev/null || true
      - name: Import project
        run: godot --headless --path . --import || true
      - name: Export Linux
        run: |
          mkdir -p build/linux
          godot --headless --path . --export-release "Linux" build/linux/tidepool.x86_64
      - name: Export macOS
        run: |
          mkdir -p build/macos
          godot --headless --path . --export-release "macOS" build/macos/tidepool.zip
      - name: Package
        run: |
          cd build && zip -r tidepool-linux-${GITHUB_REF_NAME:-manual}.zip linux
          cp macos/tidepool.zip tidepool-macos-${GITHUB_REF_NAME:-manual}.zip
      - name: Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: |
            build/tidepool-linux-*.zip
            build/tidepool-macos-*.zip
      - name: Upload artifacts (manual runs)
        if: "!startsWith(github.ref, 'refs/tags/v')"
        uses: actions/upload-artifact@v4
        with:
          name: tidepool-builds
          path: build/*.zip
